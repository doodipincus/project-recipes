const getAllProducts = (par)=>{
    if(par) return 'secess demo server + filters'
    return 'secess demo server'
}
const getProductById = (par)=>{
    if(par) return 'secess demo server + filters'
    return 'secess demo server'
}
const updateOrInsert = (par)=>{
    if(par) return 'secess demo server + filters'
    return 'secess demo server'
}

const productService = {
    getAllProducts,
    getProductById,
    updateOrInsert,
  };
  
  export default productService;